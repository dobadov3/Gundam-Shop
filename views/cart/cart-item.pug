tr 
    td
        a(href="#")
            i(class="fal fa-times-circle")
    td
        a(href="") 
            img.img-fluid.img-wishlist(src=product.image[0], alt="")
    td= product.name
    td= product.price.toLocaleString('vi', {style : 'currency', currency : 'VND'})	
    td
        input(type="number", value=product.countCart, min="1")
    td= product.total.toLocaleString('vi', {style : 'currency', currency : 'VND'})	